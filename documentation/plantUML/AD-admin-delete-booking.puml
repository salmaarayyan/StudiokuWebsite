@startuml AD Admin Delete Booking
title Activity Diagram - Admin Delete Booking

|Admin|
start
:Pilih booking yang akan dihapus;
:Klik tombol Delete;

|Sistem|
:Tampilkan popup konfirmasi
"Apakah Anda yakin ingin
menghapus booking ini?";

|Admin|
if (Konfirmasi hapus?) then (tidak)
  :Klik Batal;
  |Sistem|
  :Tutup popup;
  |Admin|
  :Kembali ke list booking;
  stop
else (ya)
  :Klik Hapus;
  
  |Database|
  :DELETE FROM bookings WHERE id = ?;
  
  |Sistem|
  if (Delete berhasil?) then (ya)
    :Return success;
    :Tampilkan notifikasi
    "Booking berhasil dihapus";
    |Database|
    :SELECT * FROM bookings;
    |Sistem|
    :Return data terbaru;
    :Refresh list booking;
  else (tidak)
    :Return error;
    :Tampilkan pesan error
    dari response API;
    |Admin|
    :Melihat pesan error;
    stop
  endif
  
  |Admin|
  :Melihat list booking
  yang sudah diupdate;
  stop
endif

@enduml
